version: "3.9"

services:
  bot:
    build: .
    restart: always
    env_file:
      - .env
    depends_on:
      - mongodb

  mongodb:
    image: mongo:7
    restart: always
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
